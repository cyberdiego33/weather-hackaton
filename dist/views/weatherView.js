export function renderWeather(message) {
    const el = document.createElement("p");
    el.textContent = `Weather says: ${message}`;
    document.body.appendChild(el);
}
//# sourceMappingURL=weatherView.js.map